import { ok,fail,handlerError } from '@/lib/api';import { prisma } from '@/lib/db';import { requireUser } from '@/lib/security/auth';import { finalizeAttempt } from '@/lib/scoring/attempts';
export async function POST(_:Request,{params}:{params:Promise<{id:string}>}){try{const u=await requireUser();const {id}=await params;const a=await prisma.attempt.findUnique({where:{id}});if(!a||a.userId!==u.id)return fail('Attempt not found',404);const saved=await finalizeAttempt(id,a.deadlineAt&&new Date()>a.deadlineAt?'AUTO_SUBMITTED':'SUBMITTED');return ok(saved)}catch(e){return handlerError(e)}}
