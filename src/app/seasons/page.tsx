export default function Seasons(){return <main className="mx-auto max-w-6xl p-6"><h1 className="text-3xl font-bold">Seasons</h1><p className="mt-3 text-slate-300">Published and unlocked seasons load through <code>/api/user/seasons</code>. Start attempts with <code>POST /api/attempts</code>.</p><div className="card mt-6"><h2 className="text-xl font-bold">Quiz rules</h2><p className="mt-2">You can change answers before final submission. No correct/wrong feedback is shown until the complete attempt is submitted.</p></div></main>}
