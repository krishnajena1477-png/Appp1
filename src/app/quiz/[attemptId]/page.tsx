export default function Quiz({params}:{params:{attemptId:string}}){return <main className="mx-auto max-w-6xl p-6"><h1 className="text-3xl font-bold">Quiz Attempt</h1><p className="mt-3 text-slate-300">Attempt ID: {params.attemptId}</p><div className="card mt-6">Use <code>GET /api/attempts/{params.attemptId}</code> for safe question payloads, then save answers with the answer API. The payload never includes correct answer metadata.</div></main>}
